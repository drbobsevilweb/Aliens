# Handoff

Current ownership and do-not-touch coordination only. Keep this file short and current.

## Active Ownership

| Area | Role | Model | Session | Status | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 20-agent mixed audit + runtime triage | Coding AI | GPT-5.4 | 2026-04-12-20-agent-audit | Completed | Applied a narrow runtime/bot/perf polish pass: hardened `scripts/play_bot.mjs` follower snapshots against stale follower refs, aligned follower idle sweep anchors with `SquadSystem` role sectors, removed duplicate direct gunfire aggro fan-out from `src/scenes/GameScene.js`, updated stale hostile-contact wording in `src/systems/MissionFlow.js`, and removed per-frame tracker filter allocations in `src/ui/MotionTracker.js`; validated with `node --check`, `node scripts/test_motion_tracker_classification.mjs`, `node scripts/test_authored_spawn_runtime.mjs`, and `node scripts/play_bot.mjs m1` (which currently clears stock `m1` as a zero-contact victory because built-in M1 still intentionally resolves fail-closed with zero authored alien spawns) |
| Strategy-research tracker contact classification | Coding AI | GPT-5.4 | 2026-04-11-tracker-classification | Completed | Continued the strategy-shooter research implementation with a low-noise pre-contact intel pass: `src/ui/MotionTracker.js` now classifies cone contacts into confirmed/tracked/vent/uncertain states and shifts the existing tracker readout color/tint accordingly, `src/scenes/GameScene.js` now exports tracker classification in automation/debug state, and added headless regression `scripts/test_motion_tracker_classification.mjs`; validated with `node scripts/test_motion_tracker_classification.mjs` and `node scripts/test-play-bot-telemetry.mjs` |
| Strategy-research tactical directive compliance | Coding AI | GPT-5.4 | 2026-04-11-strategy-directives | Completed | Ran a supervised 21-agent research pass on top-down shooters with strategy elements, then applied the narrowest high-value gameplay bundle: `src/systems/CommanderSystem.js` now exposes reusable directive tactical profiles, `src/scenes/GameScene.js` command formation now responds to hold/split/fallback spacing and catch-up differences, and `src/systems/FollowerCombatSystem.js` now uses directive lane compliance for faster on-lane reaction plus runtime-configured support suppression windows; validated with `node scripts/test-follower-ai.mjs` and new headless spec `scripts/test_commander_directive_tactics.mjs` |
| Graph sound runtime + Node SVG Actions parity | Coding AI | GPT-5.4 | 2026-04-11-graph-sound-svg | Completed | Fixed `play_sound` graph actions so `src/scenes/GameScene.js` routes runtime sound keys through `src/audio/SfxEngine.js`, added full Node `/api/svg-actions` CRUD parity plus `/api/tiled-build` command parity in `server.js`, added focused regression `scripts/test_action_graph_runtime_execution.mjs`, and revalidated the existing `scripts/test_actions_visibility_and_sound_picker.mjs` editor flow |
| Story-point event hook + backend parity cleanup | Coding AI | GPT-5.4 | 2026-04-11-story-backend-parity | Completed | `src/scenes/GameScene.js` now emits story-point runtime events into the node-graph system, `editors/tabs/story.js` exposes those events in the Actions authoring UI, `server.js` now serves `/api/editor-test-map` plus `/api/svg-assets/list` compat, and `dev_server.py` now matches the legacy `/api/save-hud-config`, `/api/save-sound`, `/api/audio-upload`, `/api/editor-test-map`, and `/api/svg-assets/list` flows used by the live editor/game surfaces; updated `README.md` and `docs/gameplay-reference.md` to remove stale wave-first wording |
| Current-state audit + markdown cleanup | Coding AI | GPT-5.4 | 2026-04-11-current-state | Completed | Ran a supervised 20-agent read-only audit across the live codebase, added `plan/current-state.md`, removed stale historical docs (`resume.md`, `md/resume-vscode-dev-2026-04-07.md`, `prompts/tasks.md`, `editors/backend/README.md`), and updated `CLAUDE.md` so backlog/docs align more closely with the actual live runtime/editor state |
| Mission authored-spawn-only flow + leader damage/close-fire fix | Coding AI | GPT-5.4 | 2026-04-09-leader-fire-m1 | Completed | `src/map/missionLayout.js` and `src/scenes/GameScene.js` now use authored alien spawn points as the only mission enemy source, suppress ambient/director backfill globally, and fail closed when a mission has zero spawn points; `src/entities/TeamLeader.js` + `src/systems/EnemyTargeting.js` now restore shared marine damage feedback/coherent leader death state, and `src/scenes/GameScene.js` fires leader shots from the real muzzle; updated `src/ui/ObjectivesPanel.js` + `src/ui/ControlsOverlay.js` to remove stale wave wording; added/updated `scripts/test_leader_damage_and_close_fire.mjs`, `scripts/test_noaliens_spawn_suppression.mjs`, `scripts/test-mission-layout.mjs`, and `scripts/verify_combat.mjs`; validated with `node --check`, targeted browser/runtime tests, and `bash ./scripts/verify.sh` |
| Recreation prompt doc | Coding AI | GPT-5.4 | 2026-04-09-recreation-prompt | Completed | Added root-level `prompty.md`, a repo-specific master build prompt for recreating the game's gameplay engine, lighting, runtime architecture, UI systems, editor surfaces, data flow, and validation expectations using placeholder visuals and stubbed audio instead of production assets |
| Editor local-package game route | Coding AI | GPT-5.4 | 2026-04-09-editor-game-route | Completed | `editors/index.html` now opens `Game` as `/game?package=local`, so the editor shell loads published package content instead of stock `m1` fallback-wave content; `scripts/test_editor_publish.mjs` now asserts the nav href and click-through PACKAGE runtime, validated with `node --check scripts/test_editor_publish.mjs` and `node scripts/test_editor_publish.mjs` |
| No-aliens + zero-spawn suppression audit | Coding AI | GPT-5.4 | 2026-04-09-no-spawn-suppression | Completed | `src/scenes/GameScene.js`, `src/systems/EnemySpawner.js`, and `src/systems/ReinforcementSystem.js` now make `?noaliens` fail closed for all enemy creation and suppress ambient vent/gunfire/director backfill spawns on package-authored maps with zero spawn points; validated with `node --check`, `node scripts/test_noaliens_spawn_suppression.mjs`, and `node scripts/test_authored_spawn_runtime.mjs` |
| M1 play bot eval hardening | Coding AI | GPT-5.4 | 2026-04-09-playbot-hardening | Completed | `scripts/play_bot.mjs` now uses direct local combat/retreat movement plus cached strategic repaths so late-combat headless runs no longer die as `state_lost`; validated with `node --check scripts/play_bot.mjs` and `node scripts/play_bot.mjs m1`, which now ends in a real gameplay defeat instead of eval loss and surfaces follower non-engagement as the remaining issue |
| Follower engagement runtime bot | Coding AI | GPT-5.4 | 2026-04-09-follower-runtime | Completed | Updated `src/systems/FollowerCombatSystem.js` so close local threats trigger the same reactive full-enemy scan as recent damage, added unit coverage in `scripts/test-follower-ai.mjs`, added focused live regression `scripts/test_follower_engagement_runtime.mjs`, and hardened `scripts/play_bot.mjs` to count only reachable nearby threats (plus busy tracker/heal/door states) before flagging idle followers; validated with `node --check`, `node scripts/test-follower-ai.mjs`, and `node scripts/test_follower_engagement_runtime.mjs` |
| Actions visibility + sound-picker regression | Coding AI | GPT-5.4 | 2026-04-09-actions-coverage | Completed | Updated `editors/tabs/missions.js`, `editors/tabs/story.js`, and `src/events/actionDefs.js` so the modular Missions surface opens the live Actions tab and `play_sound` nodes use runtime-loaded sound keys; added `scripts/test_actions_visibility_and_sound_picker.mjs` and validated with `node --check`, `node scripts/test_actions_visibility_and_sound_picker.mjs`, `node scripts/test_editors_hidden_panels.mjs`, `node scripts/test_actions_tab_phase6.mjs`, and `node scripts/test_node_graph_package.mjs` |
| Collision preview fidelity | Coding AI | GPT-5.4 | 2026-04-09-collision-preview | Completed | Added shared collision/blocker rules in `shared/tilemapCollision.js`, updated `editors/tabs/tilemaps.js` to preview terrain + door + physical-prop blockers with runtime-faithful logic, made `zone_*` props non-blocking in `src/map/MapBuilder.js` + `src/scenes/GameScene.js` + enemy/squad/spawn blocker checks, and validated with `node --check`, `node scripts/test_tilemap_collision_preview.mjs`, `node scripts/test_zone_prop_walkability.mjs`, and `node scripts/test_tilemaps_inspector_panel.mjs` |
| Lighting control parity | Coding AI | GPT-5.4 | 2026-04-08-lighting-parity | Completed | Restored the missing global `lighting.coreAlpha` settings control in `settings/index.html` and extended `scripts/test_editors_hidden_panels.mjs` so the Game tab smoke verifies the control renders and its paired inputs stay synchronized; validated with `node scripts/test-runtime-settings.mjs` and `node scripts/test_editors_hidden_panels.mjs` |
| Follower pulse overheat recovery audit/fix | Coding AI | GPT-5.4 | 2026-04-08-follower-overheat | Completed | Aligned follower pulse overheat unlock with the leader-equivalent heat threshold in `src/systems/FollowerCombatSystem.js`, added follower regression coverage in `scripts/test-follower-ai.mjs`, and validated with `node --check`, `node scripts/test-follower-ai.mjs`, and a targeted `node scripts/play_bot.mjs m1` pass |
| M1 play bot hardening | Coding AI | GPT-5.4 | 2026-04-08-m1-bot | Completed | Hardened `scripts/play_bot.mjs` against false wall/door/spawn alerts, constrained fallback mission targets to spawn-reachable tiles in `src/systems/MissionFlow.js`, added reachability coverage in `scripts/test-mission-layout.mjs`, and revalidated M1; latest bot pass no longer reports spawn/pathfinding/runtime errors, but still times out in extended combat with follower-fire issues instead of a clean mission clear |
| Combat + authored spawn regression fix | Coding AI | GPT-5.4 | 2026-04-08-combat-spawn | Completed | Fixed leader overheat muzzle-flash lockout, restored close-range alien melee damage while increasing stand-off spacing, limited zero-spawn fallback suppression to package-authored maps, expanded canonical spawn points to `count` + `enemyType` + `spawnTimeSec`, updated the tilemaps inspector/Tiled round-trip/tests, and regenerated `src/data/tiledMaps.generated.js`; validated with `node --check`, targeted spawn/weapon specs, and `bash ./scripts/verify.sh` |
| Zone lighting profiles + spawn redesign audit | Coding AI | GPT-5.4 | 2026-04-06-zone-spawn | Completed | Added modular map-editor zone presets plus localized runtime lighting softness in `editors/tabs/tilemaps.js`, `src/scenes/GameScene.js`, and `scripts/test_tilemaps_inspector_panel.mjs`; captured the requested 10-agent spawn redesign audit in `md/spawn-redesign-audit-2026-04-06.md` |
| Map editor inspector UX pass | Coding AI | Goldeneye (Preview) | 2026-04-06-map-inspector | Completed | Upgraded `editors/tabs/tilemaps.js` with a right-side Tool/Selection inspector, thumbnail-backed prop asset browser, layer-aware presets, auto-open object editing, type swapping within a layer class, and drag/numeric repositioning; added Playwright regression `scripts/test_tilemaps_inspector_panel.mjs` and aligned `prompts/map-editor.md` |
| Markdown/prompt alignment cleanup | Coding AI | Goldeneye (Preview) | 2026-04-06-doc-cleanup | Completed | Aligned the repo docs with the current modular editor/runtime layout, added `prompts/hud-editor.md`, archived superseded plans/research under `md/archive/2026-04-doc-cleanup/`, and removed stale prompt backup `prompts/mapeditor.md.save` |
| Workspace coding skill (`.github/skills/code`) | Coding AI | Goldeneye (Preview) | 2026-04-06-skill | Completed | Added a reusable repo-aware `code` skill in `.github/skills/code/SKILL.md` for minimal-diff, root-cause-first, verification-backed implementation work |
| Node-based engine migration (runtime + editor) | Coding AI | GPT-5.4 | 2026-04-04-node-engine | In Progress | Runtime/editor foundation is live across `src/events/*`, `src/scenes/GameScene.js`, `src/settings/missionPackageRuntime.js`, `editors/tabs/story.js`, and `md/*`; the 2026-04-06 pass fixed `nodeGraphs` package/schema parity in `editors/backend/js/buildPackageFromEditorState.js`, `editors/backend/js/normalizeMissionPackage.js`, and `editors/backend/schemas/mission-package-v1.schema.json` while keeping legacy `missionGraph` compatibility |
| Tilemap color-only cleanup + placeholders | Coding AI | GPT-5.4 | 2026-04-04-color-pass | Completed | Simplified active tile rendering for testing in `src/scenes/BootScene.js`, `src/map/MapBuilder.js`, `editors/tabs/tilemaps.js`, and `editors/app.js`: flat-color procedural terrain is now the active path, runtime terrain texture overlays/floor attachment previewing were removed from the live path, the modular tilemaps sidebar texture preview path was dropped, and missing authored art now renders as square placeholders instead of vanishing; validated with Problems checks on all four files |
| Image editor SVG integration + asset cleanup | Coding AI | GPT-5.4 | 2026-04-04-svg-editor | Completed | Added the live SVG sub-editor under the modular Image tab via `editors/tabs/shared/svgEditor.js` wired through `editors/tabs/sprites.js`, preserved full sprite registry fields plus new `svgAssets` metadata in `server.js`, added `/api/svg-assets*` endpoints, aligned marine reference loading with `data/sprite_registry.json`, extracted shared asset utilities for `editors/tabs/texture.js` and `editors/tabs/sprites.js`, restyled SVG surfaces in `editors/styles.css`, renamed the shell tab in `editors/index.html`, then extended the SVG document model with persisted usage presets/targets/notes for gibs, blood, and acid effects across both `server.js` and `dev_server.py`; validated with `node --check` plus browser save/reload/delete sweeps on fresh Node `8195` and Python `8196` servers |
| Spawn system redesign (Phase 1 + 2) | Editor/Map/Mechanic AI | Claude | 2026-04-03-spawn | Completed | Parallel implementation: EDITOR agent added spawn count UI to `editors/app.js` (count selector + Tiled roundtrip); MAP agent wired spawn points through `src/map/missionLayout.js` + `src/data/tilemapTemplates.js` + `src/settings/missionPackageRuntime.js`; MECHANIC agent added `spawnFromAuthoredPoints()` and dynamic spawn integration to `src/systems/EnemySpawner.js` + `src/scenes/GameScene.js`. All syntax passes, all regression specs pass, backward compatible (existing wave-based spawning still works). |
| HUD modular editor restoration | Coding AI | GPT-5.4 | 2026-04-03-hud-modular | Completed | Added the missing `editors/tabs/hud.js` module referenced by the modular shell, exposed `mapPanel` and `missionLog` alongside marine cards/objectives, added a modal inner-element editor that saves `_subs` through `/api/hud-config`, and fixed `src/ui/Minimap.js` to read the live `mapPanel` key; validated with `node --check editors/tabs/hud.js` and `node --check src/ui/Minimap.js` |
| Cross-domain source audit | Coding AI | GPT-5.4 | 2026-03-30-audit | Completed | Fixed objective target flow, persistent mission/director lighting overrides, hidden-enemy reveal/pressure leakage, and background music wiring in `src/scenes/GameScene.js`, `src/systems/MissionFlow.js`, `src/systems/EnemyDetection.js`, `src/scenes/BootScene.js`, and `src/settings/runtimeSettings.js`; `bash ./scripts/verify.sh` now only fails the separately claimed pulse-rifle timing spec |
| Pulse rifle heat/recharge tuning | Coding AI | GPT-5.4 | 2026-03-28-ammo | In Progress | Updating `src/systems/WeaponManager.js` so pulse-rifle ammo does not regenerate while the trigger is held; validating state-machine-only change |
| HUD pulse ammo drain visibility | Coding AI | GPT-5.4 | 2026-03-28-ammo | In Progress | Updating `src/ui/HUD.js` so the leader pulse-rifle readout visibly decrements while firing, flashes hotter while shooting, warns at 15 ammo, and shows a release-to-cool cue during overheat; validating HUD-only sync change |
| Runtime audit remediation | Coding AI | GPT-5.4 | 2026-03-15-remediation | Completed | Fixed combat occlusion regression, mission package/runtime field loss, extraction-phase spawn leakage, door-rule regressions, HUD input duplication, and authored spawn-count handling in `src/scenes/GameScene.js`, `src/settings/missionPackageRuntime.js`, `src/map/missionLayout.js`, `src/systems/EnemySpawner.js`, `src/entities/DoorManager.js`, `src/entities/TeamLeader.js`, `src/systems/EnemyManager.js`, `src/systems/EnemyDetection.js`, `src/systems/InputHandler.js`, `src/systems/ReinforcementSystem.js`, `src/systems/SetpieceSystem.js`, `scripts/test-mission-layout.mjs`, and `md/*`; `bash ./scripts/verify.sh` passes |
| HUD editor fidelity pass | Coding AI | GPT-5.4 | 2026-03-20-hud-editor | Completed | `src/ui/HUD.js` now consumes editor-authored card sub-element layout/style overrides for video, labels, EKG, action bar, buttons, and overheat state, and `editors/app.js` now exposes 1px HUD adjustments plus magazine preview/config; validated with Problems check and `node scripts/test_editor_runtime_fidelity.mjs` |
| HUD-local interference overlays | Coding AI | GPT-5.4 | 2026-03-20-hud-interference | Completed | Kept interference video scoped to HUD surfaces only by switching the minimap card to a local random-fade overlay in `src/ui/Minimap.js`, preserving marine-card localized glitch overlays in `src/ui/HUD.js`, and restricting `src/ui/ObjectivesPanel.js` interference to only the visible objectives panel |
| Torch brightness + ammo counter glow | Coding AI | GPT-5.4 | 2026-03-20-render-tweak | Completed | Reduced marine torch brightness and increased firing-state HUD ammo glow in `src/scenes/GameScene.js`, `src/lighting/LightingOverlay.js`, and `src/ui/HUD.js`; validated with `node --check` on all three files |
| Shader precision + post-FX fallback alignment | Coding AI | GPT-5.4 | 2026-03-20-render-fx | Completed | Switched main post-FX fragment shaders to `highp` with `mediump` fallback and aligned `GameScene` post-FX runtime fallbacks with `DEFAULT_RUNTIME_SETTINGS.graphics` in `src/scenes/GameScene.js`, `src/graphics/AlienTonePipeline.js`, `src/graphics/TiltShiftPipeline.js`, and `src/graphics/ScanlinePipeline.js`; validated with `node --check` on all four files |
| Map tile editor audit + bot | Coding AI | GPT-5.4 | 2026-03-15-editor-bot | Completed | Added tilemap QoL shortcuts and `Mirror Y` in `editors/app.js`, created `scripts/test_map_tile_editor_bot.mjs`, hardened `scripts/test_editor_object_roundtrip.mjs` for the collapsible tilemap UI, and validated with `node scripts/test_map_tile_editor_bot.mjs`, `node scripts/test_editor_publish.mjs`, `node scripts/test_editor_object_roundtrip.mjs`, and `node scripts/test_editor_runtime_fidelity.mjs` |
| Documentation and workflow docs | Documentation AI | Codex | 2026-03-10-docs | Completed | Canonical docs reconciled under `md/` |
| Alien hit / acid FX | Effects AI | Codex | 2026-03-10-acid | Completed | Increased visible acid splatter on non-lethal alien hits |
| HUD portrait + layout | Gameplay AI | Claude | 2026-03-11-hud | Completed | Portrait video feed, VITALS/ammo repositioned, EKG smoothed in `src/ui/HUD.js` |
| Audio activation + ambient | Sound AI | Claude | 2026-03-11-audio | Completed | SfxEngine enabled, ventilation hum + pipe groans + distant thumps in `AtmosphereSystem.js` |
| Enemy AI tactics | Enemies AI | Claude | 2026-03-11-ai | Completed | Warrior flank steering, probe-and-retreat in `EnemyMovement.js`, `EnemyManager.js` |
| Emergency lighting + weld sparks | Shaders AI | Claude | 2026-03-11-fx | Completed | Ambient tint in `LightingOverlay.js`, weld VFX in `DoorManager.js`, `GameScene.js` |
| HUD rebuild — Video-First refined | Gameplay AI | Gemini | 2026-03-11 | Completed | 200px width, 88% opacity, blue tint, alignment with mockup2.png |
| HUD/runtime repair | Coding AI | Codex | 2026-03-11-hud-fix | Completed | Updated `src/ui/HUD.js`, `src/scenes/GameScene.js`, `dev_server.py`, and `md/*` to remove the HUD startup crash, restore follower ammo/reload state, tighten the HUD toward `mockup2.png`, and force no-cache headers in dev |
| Regression rollback | Coding AI | Codex | 2026-03-11-regression-fix | Completed | Updated `src/systems/SquadSystem.js`, `src/scenes/GameScene.js`, `src/entities/TeamLeader.js`, `src/graphics/AlienTonePipeline.js`, `src/ui/HUD.js`, and `md/*` to fix the `resolveWalkablePosition` crash, remove leader overscaling, tone down the scene grade, and stop portrait video Y-squash |
| Visibility triage | Coding AI | Codex | 2026-03-11-visibility | Completed | Probed `src/settings/runtimeSettings.js`, `src/lighting/LightingOverlay.js`, and `src/scenes/GameScene.js` with Playwright; runtime crash is gone, but scene readability is still poor and needs a deeper render-path audit beyond lighting toggles |
| Base render readability | Coding AI | Codex | 2026-03-11-render-fix | Completed | Updated `src/scenes/BootScene.js` and `src/entities/TeamLeader.js`; restored readable scene structure by replacing the dense floor/base tileset path and keeping the leader scale above the tiny rollback state |
| Baseline visual restore | Coding AI | Codex | 2026-03-11-baseline-restore | Completed | Restored `src/scenes/BootScene.js`, `src/scenes/GameScene.js`, `src/lighting/LightingOverlay.js`, `src/graphics/AlienTonePipeline.js`, `src/settings/runtimeSettings.js`, `src/entities/TeamLeader.js`, `src/ui/HUD.js`, and `src/systems/ReinforcementSystem.js` from uploaded `Aliens.zip`; visual checkpoints are `output/recovery-direct-page.png` and `output/recovery-direct-canvas.png` |
| Runtime cleanup + verification | Coding AI | Codex | 2026-03-11-runtime-cleanup | Completed | Fixed torch origin/beam rendering, reload authority, follower last-mag firing, init-overlay leakage, authored wave progression, door/heal reservation conflicts, and updated `md/*`; stale `PickupSystem.js` still needs cleanup |
| Editor audit + prompt refinement | Documentation AI | Codex | 2026-03-11-editor-docs | Completed | Updated `.claude/agents/editor.md`, `agent_prompts.md`, `CLAUDE.md`, `md/collab.md`, and `md/progress.md`; main finding is that tilemap authoring is strong but mission/director/audio authoring is still too JSON-heavy |
| Door delegation + TL lock | Mechanic AI | Gemini | 2026-03-11 | Completed | Followers execute door tasks, TL movement lock on lock actions, durations increased |
| Alien spacing audit | Enemies AI | Gemini | 2026-03-11 | Completed | melee bounce/hard push radius increased, per-type separation hardened |
| Code cleanup pass | Coding AI | Gemini | 2026-03-11 | Completed | Dead code and stale debug removed, EnemyManager TypeError fixed |
| HUD layout + portrait reflow | Coding AI | Codex | 2026-03-11-hud-reflow | Completed | `src/ui/HUD.js`, `src/ui/MissionLog.js`, `src/ui/ContextMenu.js`, and `src/systems/InputHandler.js` now use the widened left-rail cards, bottom action strip, reduced portrait opacity, blue UI palette, larger caption styling, top-left portrait anchoring, brighter DSEG Alarm digits, expanded follower button lanes, and direct button click dispatch; Playwright passes confirmed TRACK resolves to a `Still quiet.` result and HEAL no longer falls through to world input |
| M1 test-map rebuild | Coding AI | Codex | 2026-03-11-m1-layout | Completed | `src/data/tilemapTemplates.js` now uses the three-lane `Cargo Concourse` test layout for `lv1_colony_hub`, replacing the old serpentine path for feature validation |
| Alien balance + animation sync | Coding AI | Codex | 2026-03-11-balance | Completed | `src/data/enemyData.js`, `src/entities/AlienEnemy.js`, and `src/systems/EnemySpawner.js` now run weaker/faster aliens with animation time scaling tied to actual runtime speed |
| Map / spawn / follower sweep | Coding AI | Gemini | 2026-03-12 | Completed | Removed stale `PickupSystem.js`, fixed M1 spawn/occlusion regression, tripled door integrity, added door crumple FX, enabled pulse rifle/tracker audio, adjusted AR to 16:9, reduced ambient light 20%, hardened SevenSegment font loading, implemented three-mode motion tracker audio, narrowed torch beam to 0.28, processed/optimized all portrait videos (grayscale, 300px), and shifted follower buttons right |
| Alien behavior + follower AI | Enemies AI | Claude | 2026-03-12-ai | Completed | Rush-and-swipe melee oscillation in `EnemyMovement.js`, gradual fade-in ramp in `EnemyDetection.js`, chaotic facehugger leap+flee in `EnemyMovement.js`, faster follower threat detection in `FollowerCombatSystem.js`, dynamic CombatDirector spawns in `CombatDirector.js`+`EnemySpawner.js`+`GameScene.js` |
| Infrastructure + Gemini tasks | Coding AI | Claude | 2026-03-12-infra | Completed | Auto-redirect `/`→`/game` in `dev_server.py`, shared nav bar in `game/index.html`+`settings/index.html`+`editors/index.html`, editor auto-publish on save in `editors/app.js`, removed M1 template preference in `missionLayout.js`, `?noaliens` URL param in `GameScene.js`, video color depth reduction via ffmpeg |
| Editor map runtime fidelity | Coding AI | Codex | 2026-03-13-editor-fidelity | Completed | Updated `src/settings/missionPackageRuntime.js`, `src/map/MapBuilder.js`, `src/scenes/GameScene.js`, `scripts/test_editor_runtime_fidelity.mjs`, `editors/index.html`, and `editors/styles.css` so published editor maps render authored runtime details and the editor surface has a cleaner presentation |
| Audio Overhaul & Door Fixes | Coding AI | Gemini | 2026-03-13-audio | Completed | Updated `SfxEngine.js`, `GameScene.js`, `BootScene.js`, `DoorActionSystem.js`, `EnemyManager.js`, and `config.js` to reduce volume 50%, implement 6-stage tracker urgency, add movie-accurate Aliens (1986) SFX, and fix "Unweld"/"Lock" progress bars |


## Pending Task Delegation (2026-03-12)

See `md/archive/2026-04-doc-cleanup/plan-v2-feedback.md` for historical details. All 13 items from user playtesting feedback are COMPLETE.

**Remaining**: None — all v2 feedback items + spawn redesign are done.

| Follower combat AI audit | Friendlies AI | Claude | 2026-03-13-follower-ai | Completed | Fixed burst fire, reaction delay, isDying safety, tactical reload, close-range emergency detection, facehugger priority, callout events in `src/systems/FollowerCombatSystem.js` |

## Do Not Touch Without Re-Claiming

- Shared editor/runtime fidelity paths recently changed: `src/settings/missionPackageRuntime.js`, `src/map/MapBuilder.js`, `src/scenes/GameScene.js`, `editors/index.html`, `editors/styles.css`
- `scripts/verify.sh` — do not modify without re-claiming

## Next Handoff Expectation

When you start work, replace or update the relevant row above with:

- role
- model
- session
- area
- status
- exact paths if the work touches a shared subsystem
